spring.application.name=backend
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# DadaBase config
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:gabarito}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123}
#spring.datasource.url=jdbc:postgresql://localhost:5432/gabarito
#spring.datasource.username=postgres
#spring.datasource.password=123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
api.security.token.secret=${JWT_SECRET:if-ouro-branco-mg}
#api.security.token.secret=whatever
